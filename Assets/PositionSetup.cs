using UnityEngine;
using System.Collections;

public class PositionSetup : MonoBehaviour {
    public bool m_isP1Text;
    public GameObject m_placeHolder;
    // Use this for initialization
    void Start () {
        Screen.a
        if (m_isP1Text)
        {
            gameObject.transform.position = ;
        }
        else
        {
            gameObject.transform.position = m_placeHolder.transform.position;

        }
        
	}
}
